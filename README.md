# SRV Control Center

Repository automatically created from the current running server.

GitHub owner:

`filosoff999`

Original project path:

`/opt/srv-control`

Branches:

- `main` — desired SRV Control Center version
- `server-state` — actual sanitized server state

Sensitive runtime files are intentionally excluded.

Remote deployment remains disabled until a later commit adds:

`deploy/deploy.sh`
